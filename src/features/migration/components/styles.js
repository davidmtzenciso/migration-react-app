

export const styles = theme => ({
  root: {
    flexGrow: 1,
    width: 'auto',
    height: 'auto',
    backgroundColor: '#EDF2FF'
  },
  box: {
    textAlign: 'center',
    color: theme.palette.text.primary,
  },
  paper: {
    textAlign: 'center',
    color: theme.palette.text.primary,
  },
  field: {
    color: theme.palette.text.primary,
  },
  container: {
    height: '15vh',
  }
});
